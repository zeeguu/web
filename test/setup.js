// jest-dom's matchers (toHaveAttribute, toBeInTheDocument, ...) for every test
// file. Registered here rather than imported per file so a new component test
// does not have to remember it.
import "@testing-library/jest-dom/vitest";
