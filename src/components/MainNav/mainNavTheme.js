import { darkBlue, orange600 } from "../colors";

const studentTheme = {
  primary: `${orange600}`,
};

const TeacherTheme = {
  primary: `${darkBlue}`,
};

export const theme = {
  student: studentTheme,
  teacher: TeacherTheme,
};
