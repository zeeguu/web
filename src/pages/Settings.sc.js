import styled from "styled-components";

const StyledSettings = styled.div`
  .current-class-of-student {
    margin: 25px 0 -5px 0;
  }

  .change-class-string {
    padding-top: 1rem;
  }
`;

const classContainer = styled.div`
  display: flex;
  align-items: center;
  flex-wrap: wrap;
`;

export { StyledSettings, classContainer };
